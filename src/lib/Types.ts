export type Cell = {
    x: number;
    y: number;
};

export enum Direction {
    Up,
    Down,
    Left,
    Right
}

export enum Keys {
    Space = 32,
    Left = 37,
    Up = 38,
    Right = 39,
    Down = 40
}